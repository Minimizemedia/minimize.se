import React from 'react';
import SideNav from '../components/SideNav';

const Work = () => (
  <>
    <div>Work</div>
    <SideNav
      title={'start'}
      position={'left'}
      nextPage={'/index'}
      bgColor={'pale'}
      titleColor={'sage'}
    />
  </>
);

export default Work;
