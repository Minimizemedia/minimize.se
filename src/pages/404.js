import React from 'react';
// import Layout from '../components/layout';

const NotFoundPage = () => (
  // <Layout>
  <div style={{
    backgroundColor: '#eddfc6',
    width: '100vw',
    height: '100vh',
    display: 'flex',
    justifyContent: 'center',
    alignContent: 'center',
    alignItems: 'center',
    flexDirection: 'column'
  }}>
    <h1 style={{
      color: '#00398d'
    }}>not_found</h1>
  </div>
  // </Layout>
);

export default NotFoundPage;
